name: {{PROJECT_NAME}}
metadata:
  template: cosmos-agent-starter@0.5.0
services:
  api:
    project: .
    host: containerapp
    language: docker
    docker:
      path: ./Dockerfile
      context: .
infra:
  provider: bicep
  path: infra
